# MongoDB Setup

This folder is intentionally minimal.

## Automated Creation
MongoDB database and collections are created automatically through the Python ETL script:
```
python scripts/etl.py <mongo_uri>
```